# Custom error pages for your Nuxt app, if desired.
# Note: InfinityFree's cPanel also has a way to configure this.
ErrorDocument 404 /404.html
ErrorDocument 400 /404.html
ErrorDocument 403 /404.html

# Turn on the Rewrite Engine
RewriteEngine On

# -----------------
# Nuxt Static File and Directory Handling
# -----------------
# If the requested file or directory exists, serve it directly.
# This is crucial for your Nuxt app's static assets (JS, CSS, images).
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# -----------------
# PHP API Front Controller
# -----------------
# All other requests (e.g., API calls) are rewritten to all-routes.php.
RewriteRule ^ catch-all.php [QSA,L]
